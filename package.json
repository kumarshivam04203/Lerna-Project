{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  },
  "scripts": {
    "start": "lerna run start --parallel",
    "test": "lerna run test --parallel",
    "build": "lerna run build"
  }
}
